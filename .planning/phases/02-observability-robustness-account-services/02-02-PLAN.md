---
phase: 02-observability-robustness-account-services
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [pkg/payoneer/accounts.go, pkg/payoneer/transactions.go, pkg/payoneer/options.go, pkg/payoneer/service.go, pkg/payoneer/client_integration_test.go]
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03]
must_haves:
  truths:
    - "User can retrieve account balances for multiple currencies"
    - "User can list, filter, and paginate transaction history"
    - "Transaction details can be retrieved by ID"
    - "Business operations produce semantic OpenTelemetry spans"
  artifacts:
    - path: "pkg/payoneer/accounts.go"
      provides: "AccountsService.GetBalances implementation and Balance models"
    - path: "pkg/payoneer/transactions.go"
      provides: "AccountsService.ListTransactions and Transaction models"
  key_links:
    - from: "pkg/payoneer/accounts.go"
      to: "GET /v2/accounts/{id}/balances"
      via: "client.Do()"
    - from: "pkg/payoneer/transactions.go"
      to: "GET /v2/accounts/{id}/transactions"
      via: "client.Do() with query params"
---

<objective>
Implement core Account Services for retrieving balances and transactions. 
Provide type-safe models for financial data (using cents for amounts) and support for filtering and pagination.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability-robustness-account-services/02-01-SUMMARY.md
@pkg/payoneer/client.go
@pkg/payoneer/service.go
@pkg/payoneer/options.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Account Balances Implementation</name>
  <files>pkg/payoneer/accounts.go, pkg/payoneer/service.go, pkg/payoneer/client_integration_test.go</files>
  <action>
    - Define `Balance` struct with `Currency string` and `Amount int64` (cents).
    - Implement `GetBalances(ctx context.Context, accountID string) ([]Balance, error)` on `AccountsService`.
    - Create a semantic OTel span `payoneer.account.get_balances` with the `account_id` attribute.
    - Ensure `Client.Do` is used for the request to `/v2/accounts/{accountID}/balances`.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer -v -run TestAccountsGetBalances</automated>
  </verify>
  <done>User can retrieve balances for a specific Payoneer account.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction History & Details</name>
  <files>pkg/payoneer/transactions.go, pkg/payoneer/options.go, pkg/payoneer/client_integration_test.go</files>
  <action>
    - Define `Transaction` struct and related types (Status, Direction, etc.).
    - Implement `ListTransactions(ctx context.Context, accountID string, opts ...TransactionListOption) ([]Transaction, error)` on `AccountsService`.
    - Implement `GetTransaction(ctx context.Context, accountID string, transactionID string) (*Transaction, error)` on `AccountsService`.
    - Implement `TransactionListOption` functional options: `WithPage`, `WithPageSize`, `WithFrom`, `WithTo`, `WithStatus`.
    - Use semantic OTel spans for both methods.
    - Map options to query parameters in the request to `/v2/accounts/{accountID}/transactions`.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer -v -run TestAccountsTransactions</automated>
  </verify>
  <done>User can list transaction history with filtering and pagination, and retrieve specific transaction details.</done>
</task>

</tasks>

<success_criteria>
1. Balances are correctly returned as cents (int64) to avoid floating point issues.
2. Transaction filtering and pagination work as expected with the correct query parameters.
3. OpenTelemetry spans correctly capture business-level operations.
4. Integration tests (using Sandbox or Mock) verify the full request/response cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-robustness-account-services/02-02-SUMMARY.md`
</output>
