---
phase: 02-observability-robustness-account-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pkg/payoneer/client.go, pkg/payoneer/options.go, pkg/payoneer/optional.go, pkg/payoneer/client_test.go]
autonomous: true
requirements: [OBS-03, HTTP-01, HTTP-02]
must_haves:
  truths:
    - "Client automatically retries failed requests with exponential backoff"
    - "HTTP requests generate OpenTelemetry spans and metrics"
    - "Retry logs are redacted and routed through the SDK's logger"
  artifacts:
    - path: "pkg/payoneer/optional.go"
      provides: "Generic Optional wrapper for nullable API fields"
    - path: "pkg/payoneer/options.go"
      provides: "WithRetries, WithTracerProvider, and WithMeterProvider functional options"
  key_links:
    - from: "pkg/payoneer/client.go"
      to: "github.com/hashicorp/go-retryablehttp"
      via: "retryablehttp.StandardClient()"
    - from: "pkg/payoneer/client.go"
      to: "go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp"
      via: "otelhttp.NewTransport()"
---

<objective>
Implement SDK robustness via automatic retries and observability via OpenTelemetry. 
Integrate `go-retryablehttp` for reliable network operations and `otelhttp` for automated telemetry.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability-robustness-account-services/02-CONTEXT.md
@.planning/phases/02-observability-robustness-account-services/02-RESEARCH.md
@pkg/payoneer/client.go
@pkg/payoneer/options.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Robust Retry Logic</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options.go, pkg/payoneer/client_test.go</files>
  <action>
    - Integrate `github.com/hashicorp/go-retryablehttp` into the `Client`.
    - Add `WithRetries(max int)` and `WithRetryWait(min, max time.Duration)` options to `pkg/payoneer/options.go`.
    - Update `NewClient` to initialize a `retryablehttp.Client` when `WithRetries` is used.
    - Implement a `LeveledLogger` adapter to route `retryablehttp` logs through `c.Logger`.
    - Ensure `Authenticate` method also benefits from retries by wrapping its inner client's transport.
    - ORDER: Base Transport -> Retryable Transport -> Logging Transport.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer -v -run TestClientRetry</automated>
  </verify>
  <done>Client automatically retries 5xx and 429 status codes with exponential backoff.</done>
</task>

<task type="auto">
  <name>Task 2: OpenTelemetry Instrumentation</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options.go, pkg/payoneer/client_test.go</files>
  <action>
    - Add `tracerProvider trace.TracerProvider` and `meterProvider metric.MeterProvider` fields to `Client`.
    - Add `WithTracerProvider(tp trace.TracerProvider)` and `WithMeterProvider(mp metric.MeterProvider)` functional options.
    - In `NewClient`, if providers are present, wrap the HTTP transport with `otelhttp.NewTransport`.
    - Initialize an internal `tracer` in the `Client` using the provider.
    - Update `Authenticate` to preserve OTel instrumentation on the authenticated client.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer -v -run TestClientOTel</automated>
  </verify>
  <done>Outgoing requests generate OTel spans and metrics when providers are configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generic Optional Wrapper</name>
  <files>pkg/payoneer/optional.go, pkg/payoneer/optional_test.go</files>
  <behavior>
    - Some[T](v) returns an Optional with value v and Valid=true.
    - None[T]() returns an Optional with Valid=false.
    - Get() returns value and ok flag.
    - OrDefault(d) returns value if ok, else d.
    - JSON Marshaling/Unmarshaling support (Optional[T] should marshal to its value if ok, else null).
  </behavior>
  <action>
    - Implement the `Optional[T]` generic struct and helper functions in `pkg/payoneer/optional.go`.
    - Implement `json.Marshaler` and `json.Unmarshaler` interfaces for `Optional[T]`.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer -v -run TestOptional</automated>
  </verify>
  <done>A robust Optional[T] wrapper is available for API data modeling.</done>
</task>

</tasks>

<success_criteria>
1. All unit tests for retries and OTel instrumentation pass.
2. Optional[T] correctly handles JSON serialization and deserialization.
3. Client configuration allows setting retry limits and telemetry providers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-robustness-account-services/02-01-SUMMARY.md`
</output>
