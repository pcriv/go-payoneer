---
phase: 01-client-foundation-authentication
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - pkg/payoneer/errors.go
  - internal/transport/redaction.go
  - internal/transport/response.go
  - pkg/payoneer/client.go
autonomous: true
requirements: [ERR-01, OBS-02]

must_haves:
  truths:
    - "Non-2xx API responses return structured APIError"
    - "False Positive 200 OK responses with business errors return APIError"
    - "Sensitive headers (Authorization) are redacted in logs"
    - "Sensitive body fields (tokens, secrets) are redacted in logs"
  artifacts:
    - path: "pkg/payoneer/errors.go"
      provides: "APIError struct and code mappings"
    - path: "internal/transport/redaction.go"
      provides: "slog redaction handler"
    - path: "internal/transport/response.go"
      provides: "Response validation logic for false positives"
  key_links:
    - from: "internal/transport/response.go"
      to: "pkg/payoneer/errors.go"
      via: "error mapping"
    - from: "internal/transport/redaction.go"
      to: "pkg/payoneer/client.go"
      via: "logger configuration"
---

<objective>
Implement custom error handling for Payoneer's specific API patterns and a robust redaction mechanism for structured logging.

Purpose: Ensure clear error diagnostic and security compliance for all logs.
Output: Custom error types and a redaction-aware slog handler.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-client-foundation-authentication/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement APIError and Response Validation</name>
  <files>pkg/payoneer/errors.go, internal/transport/response.go, internal/transport/response_test.go</files>
  <behavior>
    - Parse standard Payoneer error JSON into APIError.
    - Check for business errors in 200 OK responses (e.g., if "status" is not "Success").
    - Map specific Payoneer error codes to SDK-defined constants.
    - APIError should implement the error interface and be usable with errors.Is/As.
  </behavior>
  <action>
    - Define `APIError` struct in `pkg/payoneer/errors.go`.
    - Implement `ValidateResponse(resp *http.Response)` in `internal/transport/response.go`.
    - Include logic to peek into the response body if needed (ensure it's replaced for subsequent decoding).
  </action>
  <verify>
    <automated>go test -v ./internal/transport/...</automated>
  </verify>
  <done>Custom error handling correctly identifies both HTTP-level and business-level failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement slog Redaction Handler</name>
  <files>internal/transport/redaction.go, internal/transport/redaction_test.go</files>
  <behavior>
    - Redact "Authorization" header from any logged request.
    - Redact "client_secret", "access_token", "refresh_token" fields from logged JSON bodies.
    - Redaction should be case-insensitive for header names.
    - User should be able to extend the redaction list via functional options.
  </behavior>
  <action>
    - Implement a custom `slog.Handler` (or middleware) that wraps the default one.
    - Define a default list of redacted keys.
    - Provide a way to configure this in the `WithLogger` option (modified if necessary).
  </action>
  <verify>
    <automated>go test -v ./internal/transport/...</automated>
  </verify>
  <done>Structured logs are automatically redacted for sensitive information.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Client Transport and Logging</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/integration_test.go</files>
  <action>
    - Connect the authenticated `http.Client` to use the logging handler.
    - Ensure all SDK calls pass through the `ValidateResponse` logic.
    - Create a small integration test that simulates a mocked Payoneer request and verifies that the output is redacted in logs and errors are correctly parsed.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/...</automated>
  </verify>
  <done>All components (Auth, Transport, Logging, Errors) are fully integrated into the Client.</done>
</task>

</tasks>

<verification>
Run the integration test and inspect the logs (redirected to buffer) to ensure redaction occurred. Verify that a 400 Bad Request results in an `APIError`.
</verification>

<success_criteria>
1. Logs do not contain plain-text credentials or secrets.
2. Business errors in 200 OK responses are correctly detected.
3. Integration tests demonstrate the end-to-end flow working as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-foundation-authentication/01-03-SUMMARY.md`
</output>
