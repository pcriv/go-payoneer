---
phase: 01-client-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - pkg/payoneer/client.go
  - pkg/payoneer/options.go
  - pkg/payoneer/types.go
  - pkg/payoneer/service.go
autonomous: true
requirements: [CLNT-01, CLNT-02, CLNT-03, OBS-01]

must_haves:
  truths:
    - "Developer can initialize the client using payoneer.NewClient()"
    - "Client supports Sandbox and Production base URLs via options"
    - "Client struct exposes pre-initialized service fields (e.g., client.Accounts)"
    - "All public client methods accept context.Context as first argument"
    - "Default structured logger (slog) is used if none provided"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
    - path: "pkg/payoneer/client.go"
      provides: "Main Client struct and constructor"
    - path: "pkg/payoneer/options.go"
      provides: "Functional options for configuration"
    - path: "pkg/payoneer/service.go"
      provides: "Base service structure for future endpoints"
  key_links:
    - from: "pkg/payoneer/client.go"
      to: "pkg/payoneer/options.go"
      via: "functional options pattern in NewClient"
    - from: "pkg/payoneer/client.go"
      to: "pkg/payoneer/service.go"
      via: "initialization of service fields"
---

<objective>
Initialize the Go module and establish the core SDK structure with a functional options-based Client.

Purpose: Provide a clean, extensible entry point for the SDK that follows Go idioms.
Output: Valid go.mod, core Client struct, and configuration options.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and directory structure</name>
  <files>go.mod, pkg/payoneer/doc.go</files>
  <action>
    - Run `go mod init github.com/pablocrivella/go-payoneer`.
    - Create the base directory structure: `pkg/payoneer/`, `internal/auth/`, `internal/transport/`.
    - Add a `doc.go` file to `pkg/payoneer/` describing the package.
  </action>
  <verify>
    <automated>ls go.mod pkg/payoneer/doc.go</automated>
  </verify>
  <done>Go module initialized and core directories created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Base Client and Functional Options</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options.go, pkg/payoneer/client_test.go</files>
  <behavior>
    - NewClient() should return a client with default Production BaseURL if no options.
    - WithBaseURL(url) should override the default URL.
    - WithTimeout(duration) should set the underlying http.Client timeout.
    - WithLogger(*slog.Logger) should set the internal logger.
    - All options should be applied in order.
  </behavior>
  <action>
    - Define `Client` struct in `pkg/payoneer/client.go`.
    - Implement `Option` type and `NewClient(...Option)` constructor in `pkg/payoneer/options.go`.
    - Include `BaseURL`, `HTTPClient`, and `Logger` (slog) in the `Client` struct.
    - Use `slog.Default()` as the initial logger.
    - Ensure all public methods on `Client` (for future tasks) will accept `context.Context`.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/...</automated>
  </verify>
  <done>Client can be instantiated and configured via functional options.</done>
</task>

<task type="auto">
  <name>Task 3: Define Service-Oriented Structure</name>
  <files>pkg/payoneer/service.go, pkg/payoneer/client.go</files>
  <action>
    - Create a base `service` struct in `pkg/payoneer/service.go` that holds a reference to the `Client`.
    - Add `Accounts` and `Payouts` fields to the `Client` struct (types will be defined as pointers to empty service structs for now).
    - Initialize these services in `NewClient`.
  </action>
  <verify>
    <automated>grep -E "Accounts|Payouts" pkg/payoneer/client.go</automated>
  </verify>
  <done>Client has pre-initialized service accessors for improved discoverability.</done>
</task>

</tasks>

<verification>
Check that `go.mod` is valid, and the `payoneer.Client` can be initialized in a test with different options.
</verification>

<success_criteria>
1. `go.mod` exists with the correct module path.
2. `payoneer.NewClient` works and allows setting BaseURL, Timeout, and Logger.
3. `client.Accounts` and `client.Payouts` are accessible (though methods are not yet implemented).
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-foundation-authentication/01-01-SUMMARY.md`
</output>
