---
phase: 01-client-foundation-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/auth/provider.go
  - internal/auth/token_store.go
  - pkg/payoneer/options.go
  - pkg/payoneer/client.go
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "SDK handles OAuth 2.0 Client Credentials flow"
    - "SDK handles OAuth 2.0 Authorization Code flow"
    - "Tokens are cached and automatically refreshed when expired"
    - "Token storage is thread-safe"
  artifacts:
    - path: "internal/auth/provider.go"
      provides: "OAuth 2.0 implementation using x/oauth2"
    - path: "internal/auth/token_store.go"
      provides: "Thread-safe in-memory token storage"
  key_links:
    - from: "pkg/payoneer/client.go"
      to: "internal/auth/provider.go"
      via: "authenticated http.Client injection"
---

<objective>
Implement secure OAuth 2.0 authentication for both Client Credentials and Authorization Code flows, ensuring thread-safe token management and automatic refreshing.

Purpose: Secure all outgoing API calls with valid access tokens.
Output: Working authentication layer integrated into the base Client.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-client-foundation-authentication/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Token Management and Storage</name>
  <files>internal/auth/token_store.go, internal/auth/token_store_test.go</files>
  <behavior>
    - TokenStore.Get() should return the current token.
    - TokenStore.Set(token) should store the token in a thread-safe manner.
    - Concurrent calls to Get/Set should not cause race conditions.
  </behavior>
  <action>
    - Define `TokenStore` interface with `Get` and `Set` methods.
    - Implement `InMemoryStore` using `sync.RWMutex`.
    - Provide a default `InMemoryStore` in the auth provider.
  </action>
  <verify>
    <automated>go test -v ./internal/auth/...</automated>
  </verify>
  <done>Thread-safe token storage implemented and verified with race detector.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OAuth 2.0 Flows</name>
  <files>internal/auth/provider.go, pkg/payoneer/options.go</files>
  <action>
    - Use `golang.org/x/oauth2` to implement a provider that handles both flows.
    - Add `WithClientCredentials(id, secret)` functional option.
    - Add `WithAuthCode(id, secret, code, redirect)` functional option.
    - Ensure the provider returns an `*http.Client` that automatically handles token injection and refresh.
    - Use Payoneer's OAuth2 v2 endpoints (from research).
  </action>
  <verify>
    <automated>go test -v ./internal/auth/...</automated>
  </verify>
  <done>Authentication flows implemented and integrated with Client options.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Authentication to Client</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options_test.go</files>
  <action>
    - Update `NewClient` to initialize the authenticated `http.Client`.
    - If no auth options provided, client should return error on first request (or when trying to initialize transport).
    - Ensure the internal `Client.httpClient` is replaced by the one provided by the auth provider.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/...</automated>
  </verify>
  <done>Client successfully uses authenticated transport for API calls.</done>
</task>

</tasks>

<verification>
Verify that auth options can be set and that they correctly configure the underlying http client. Mock server can be used to verify token exchange calls.
</verification>

<success_criteria>
1. `InMemoryStore` passes concurrent access tests.
2. Auth provider correctly uses `x/oauth2` to exchange credentials for tokens.
3. Tokens are automatically injected into outgoing requests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-foundation-authentication/01-02-SUMMARY.md`
</output>
