---
phase: 03-financial-operations-payouts
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - pkg/payoneer/payouts.go
  - pkg/payoneer/payouts_test.go
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]

must_haves:
  truths:
    - "Developer can submit a mass payout request with multiple items"
    - "Developer can retrieve the status of a specific payout"
    - "Developer can cancel a pending payout"
    - "SDK identifies business failures in 200 OK responses"
  artifacts:
    - path: "pkg/payoneer/payouts.go"
      provides: "PayoutsService implementation"
    - path: "pkg/payoneer/payouts_test.go"
      provides: "Test coverage for Payout operations"
  key_links:
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{program_id}/masspayouts"
      via: "POST request"
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{program_id}/payouts/{id}/status"
      via: "GET request"
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{program_id}/payouts/{id}/cancel"
      via: "POST request"
---

<objective>
Implement the core financial operations for mass payouts, status tracking, and cancellation with robust error handling for "False Positive" 200 OK responses.

Purpose: Provide the implementation that handles communication with Payoneer Payout APIs and business validation logic.
Output: Working PayoutsService and verified test cases.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-financial-operations-payouts/03-CONTEXT.md
@.planning/phases/03-financial-operations-payouts/03-RESEARCH.md
@.planning/phases/03-financial-operations-payouts/03-01-SUMMARY.md
@pkg/payoneer/client.go
@pkg/payoneer/payout_types.go
@pkg/payoneer/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PayoutsService Methods</name>
  <files>pkg/payoneer/payouts.go</files>
  <action>
    Implement `CreateMassPayout(ctx context.Context, req *MassPayoutRequest) (*PayoutBatchResult, error)`, `GetPayoutStatus(ctx context.Context, clientReferenceID string) (*PayoutStatusResult, error)`, and `CancelPayout(ctx context.Context, clientReferenceID string) error`.
    Construct paths using the `ProgramID` from the client:
    - POST `/v4/programs/{program_id}/masspayouts`
    - GET `/v4/programs/{program_id}/payouts/{id}/status`
    - POST `/v4/programs/{program_id}/payouts/{id}/cancel`
    Handle URL encoding for `clientReferenceID`.
  </action>
  <verify>
    <automated>go build ./pkg/payoneer/payouts.go</automated>
  </verify>
  <done>Core Payout service methods are implemented and use the correct API endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Business Status Validation & Error Handling</name>
  <files>pkg/payoneer/payouts.go</files>
  <behavior>
    - `CreateMassPayout` first calls `req.Validate()`.
    - If a response is received with 200 OK but contains a business failure status (e.g., individual item errors), the result reflects this.
    - Map Payoneer-specific error code 2306 (Not Found) to a descriptive Go error.
  </behavior>
  <action>
    Implement validation and post-processing logic in `PayoutsService` methods.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/ -run TestPayoutErrorHandling</automated>
  </verify>
  <done>SDK handles transport success with business failure scenarios correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Payout Operations Tests</name>
  <files>pkg/payoneer/payouts_test.go</files>
  <action>
    Create comprehensive tests using `httptest.Server` to mock various Payoneer API scenarios:
    - Successful mass payout creation.
    - Retrieval of status for a pending/completed payout.
    - Successful cancellation.
    - 400 Validation Error from the API.
    - 404/2306 Payout Not Found.
    - 200 OK with business status indicating rejection.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/ -run TestPayoutsService</automated>
  </verify>
  <done>PayoutsService implementation is fully verified against expected API behavior.</done>
</task>

</tasks>

<verification>
Run all Payout-related tests to confirm correct endpoint interactions and error handling.
</verification>

<success_criteria>
1. `CreateMassPayout` successfully sends requests and parses responses.
2. `GetPayoutStatus` and `CancelPayout` work as expected.
3. Tests cover success, business failure, and transport error cases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-operations-payouts/03-02-SUMMARY.md`
</output>
