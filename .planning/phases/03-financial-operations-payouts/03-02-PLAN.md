---
phase: 03-financial-operations-payouts
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [pkg/payoneer/payouts.go, pkg/payoneer/payouts_test.go]
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]

must_haves:
  truths:
    - "User can submit a mass payout batch"
    - "User can check the status of a payout by its client reference ID"
    - "User can cancel a pending payout by its client reference ID"
    - "SDK correctly identifies terminal failures like 404/2306 Payout Not Found"
  artifacts:
    - path: "pkg/payoneer/payouts.go"
      provides: "PayoutsService implementation"
    - path: "pkg/payoneer/payouts_test.go"
      provides: "Tests for payout operations"
  key_links:
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{id}/masspayouts"
      via: "POST request"
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{id}/payouts/{id}/status"
      via: "GET request"
    - from: "pkg/payoneer/payouts.go"
      to: "/v4/programs/{id}/payouts/{id}/cancel"
      via: "POST request"
---

<objective>
Implement the `PayoutsService` to perform core payout operations: mass payout submission, status retrieval, and cancellation.

Purpose: Provide a clean, type-safe API for executing financial transactions with Payoneer.
Output: Working `PayoutsService` and comprehensive tests.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-financial-operations-payouts/03-CONTEXT.md
@.planning/phases/03-financial-operations-payouts/03-RESEARCH.md
@pkg/payoneer/client.go
@pkg/payoneer/payout_types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CreateMassPayout</name>
  <files>pkg/payoneer/payouts.go</files>
  <action>
    - Create `pkg/payoneer/payouts.go`.
    - Implement `CreateMassPayout(ctx context.Context, req *MassPayoutRequest) (*PayoutBatchResult, error)` on `PayoutsService`.
    - Validation: Call `req.Validate()`.
    - Cents to String: Before sending, loop through `Payments` and populate `AmountStr` using `fmt.Sprintf("%.2f", float64(cents)/100.0)`.
    - API Call: Use `POST /v4/programs/{program_id}/masspayouts`.
    - Note: Handle cases where `ProgramID` is not set on the client.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer/ -run TestCreateMassPayout</automated>
  </verify>
  <done>Mass payouts can be submitted with automatic validation and conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GetStatus and Cancel</name>
  <files>pkg/payoneer/payouts.go</files>
  <action>
    - Implement `GetStatus(ctx context.Context, clientReferenceID string) (*PayoutStatusResult, error)` on `PayoutsService`.
    - API Call: Use `GET /v4/programs/{program_id}/payouts/{client_reference_id}/status`.
    - Error Handling: Explicitly check for 404/2306 error codes and map them to a clear failure status or error.
    - Implement `Cancel(ctx context.Context, clientReferenceID string) (*PayoutCancelResult, error)` on `PayoutsService`.
    - API Call: Use `POST /v4/programs/{program_id}/payouts/{client_reference_id}/cancel`.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer/ -run TestGetStatus_Cancel</automated>
  </verify>
  <done>Users can poll status and cancel payouts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit and Integration Tests</name>
  <files>pkg/payoneer/payouts_test.go</files>
  <action>
    - Create `pkg/payoneer/payouts_test.go`.
    - Test `CreateMassPayout` success and validation failures.
    - Test `GetStatus` success and 404/2306 scenarios.
    - Test `Cancel` success and failure.
    - Test edge cases: missing ProgramID, invalid currency, amount formatting.
    - Use `httptest.NewServer` to mock the Payoneer API responses.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/payouts_test.go</automated>
  </verify>
  <done>All payout operations are covered by tests with high reliability.</done>
</task>

</tasks>

<verification>
Ensure all tests in `pkg/payoneer/payouts_test.go` pass.
Verify that `AmountStr` is correctly formatted to 2 decimal places in the request body.
Verify that `ProgramID` is correctly interpolated in the API paths.
</verification>

<success_criteria>
- `PayoutsService` is fully functional for Mass Payouts.
- Idempotency is mandatory.
- SDK handles async status polling and terminal failures (404/2306).
- All financial operations are properly tested.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-operations-payouts/03-02-SUMMARY.md`
</output>
