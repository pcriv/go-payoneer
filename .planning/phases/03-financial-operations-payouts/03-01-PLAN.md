---
phase: 03-financial-operations-payouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pkg/payoneer/client.go, pkg/payoneer/options.go, pkg/payoneer/payout_types.go]
autonomous: true
requirements: [PAY-01, PAY-04]

must_haves:
  truths:
    - "Client can be initialized with a Program ID"
    - "MassPayoutRequest correctly defines payout items and validation rules"
    - "Validation catches invalid amounts and missing reference IDs"
  artifacts:
    - path: "pkg/payoneer/payout_types.go"
      provides: "Payout request/response models"
    - path: "pkg/payoneer/options.go"
      provides: "WithProgramID functional option"
  key_links:
    - from: "pkg/payoneer/options.go"
      to: "pkg/payoneer/client.go"
      via: "WithProgramID option setting Client.ProgramID"
---

<objective>
Update the SDK client to support Program IDs and define the core data models for payout operations, including strict business validation.

Purpose: Program ID is a mandatory part of all Payout v4 endpoints. Data models with validation ensure data integrity before making API calls.
Output: Updated Client and new Payout types with validation logic.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-financial-operations-payouts/03-CONTEXT.md
@.planning/phases/03-financial-operations-payouts/03-RESEARCH.md
@pkg/payoneer/client.go
@pkg/payoneer/options.go
@pkg/payoneer/optional.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ProgramID support to Client</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options.go</files>
  <action>
    - Add `ProgramID string` field to the `Client` struct in `pkg/payoneer/client.go`.
    - Implement `WithProgramID(id string) Option` in `pkg/payoneer/options.go` to allow setting the Program ID during client initialization.
    - Ensure the field is accessible to services within the `payoneer` package.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer/ -run TestNewClient</automated>
  </verify>
  <done>Client can store and retrieve ProgramID via functional options.</done>
</task>

<task type="auto">
  <name>Task 2: Define Payout Models and Validation</name>
  <files>pkg/payoneer/payout_types.go</files>
  <action>
    - Create `pkg/payoneer/payout_types.go`.
    - Define `PayoutItem` struct with:
        - `ClientReferenceID string` (`json:"client_reference_id"`)
        - `PayeeID string` (`json:"payee_id"`)
        - `Amount int64` (`json:"-"`) - represents cents
        - `AmountStr string` (`json:"amount"`) - internal field for decimal string
        - `Currency string` (`json:"currency"`)
        - `Description string` (`json:"description,omitempty"`)
    - Define `MassPayoutRequest` struct containing `Payments []PayoutItem`.
    - Implement `func (r *MassPayoutRequest) Validate() error` on `MassPayoutRequest`:
        - Ensure at least one payment.
        - Max 500 payments.
        - Each payment must have `ClientReferenceID` and `PayeeID`.
        - Each payment `Amount` must be > 0.
    - Define response models using `Optional[T]` for nullable fields:
        - `PayoutBatchResult`: status, batch_id, etc.
        - `PayoutStatusResult`: status, amount, currency, reason (Optional), release_date (Optional).
        - `PayoutCancelResult`: status, description.
  </action>
  <verify>
    <automated>go test ./pkg/payoneer/ -run TestMassPayoutRequest_Validate</automated>
  </verify>
  <done>Payout models are defined with comprehensive validation logic.</done>
</task>

</tasks>

<verification>
Check that `Client` initialization works with `WithProgramID`.
Check that `MassPayoutRequest.Validate()` correctly identifies valid and invalid requests.
</verification>

<success_criteria>
- SDK Client supports `ProgramID`.
- Payout models handle cents-to-decimal string conversion requirements (via validation/internal fields).
- Mandatory idempotency (client_reference_id) is enforced via `Validate()`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-operations-payouts/03-01-SUMMARY.md`
</output>
