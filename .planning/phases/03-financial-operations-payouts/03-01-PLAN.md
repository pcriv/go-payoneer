---
phase: 03-financial-operations-payouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/payoneer/client.go
  - pkg/payoneer/options.go
  - pkg/payoneer/payout_types.go
autonomous: true
requirements: [PAY-01, CLNT-01]

must_haves:
  truths:
    - "Developer can configure a ProgramID on the client"
    - "SDK rejects invalid payout requests locally (e.g., missing reference ID)"
  artifacts:
    - path: "pkg/payoneer/client.go"
      provides: "ProgramID storage"
    - path: "pkg/payoneer/options.go"
      provides: "WithProgramID option"
    - path: "pkg/payoneer/payout_types.go"
      provides: "Mass Payout request/response models and validation"
  key_links:
    - from: "pkg/payoneer/payout_types.go"
      to: "MassPayoutRequest.Validate()"
      via: "Method implementation"
---

<objective>
Extend the SDK core to support Payout operations by adding ProgramID configuration and defining robust request/response models with built-in validation.

Purpose: Payout endpoints are program-specific and require strict structural validation before transmission.
Output: Configurable client and type-safe payout models.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-financial-operations-payouts/03-CONTEXT.md
@.planning/phases/03-financial-operations-payouts/03-RESEARCH.md
@pkg/payoneer/client.go
@pkg/payoneer/options.go
@pkg/payoneer/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Program ID Configuration</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/options.go</files>
  <action>
    Add `ProgramID string` to the `Client` struct in `pkg/payoneer/client.go`. 
    Implement the `WithProgramID(id string) Option` functional option in `pkg/payoneer/options.go`.
    Ensure the option is exported and properly documented.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/ -run TestNewClient</automated>
  </verify>
  <done>Client can be initialized with WithProgramID and the value is stored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Payout Models</name>
  <files>pkg/payoneer/payout_types.go</files>
  <behavior>
    - PayoutItem must have ClientReferenceID (mandatory), Amount (int64), PayeeID (string), Currency (string).
    - MassPayoutRequest must contain a slice of PayoutItem.
    - PayoutBatchResult must represent the API response for mass payouts.
    - Use Optional[T] for nullable fields like Reason or ReleaseDate.
  </behavior>
  <action>
    Create `pkg/payoneer/payout_types.go` and define the required structs. 
    Ensure json tags match Payoneer v4 API specifications.
  </action>
  <verify>
    <automated>go build ./pkg/payoneer/payout_types.go</automated>
  </verify>
  <done>Payout models are defined and compile correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Mass Payout Validation</name>
  <files>pkg/payoneer/payout_types.go</files>
  <behavior>
    - Validate() returns error if Payments slice is empty.
    - Validate() returns error if Payments slice > 500 items.
    - Validate() returns error if any item has empty ClientReferenceID.
    - Validate() returns error if any item has Amount <= 0.
  </behavior>
  <action>
    Implement `(r *MassPayoutRequest) Validate() error` method in `pkg/payoneer/payout_types.go`.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer/ -run TestMassPayoutRequest_Validate</automated>
  </verify>
  <done>Structural validation of mass payout requests is enforced locally.</done>
</task>

</tasks>

<verification>
Check that the client can be initialized with a Program ID and that the MassPayoutRequest models are correctly validated.
</verification>

<success_criteria>
1. `Client` struct contains `ProgramID`.
2. `WithProgramID` option works.
3. `MassPayoutRequest.Validate()` correctly identifies invalid requests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-operations-payouts/03-01-SUMMARY.md`
</output>
