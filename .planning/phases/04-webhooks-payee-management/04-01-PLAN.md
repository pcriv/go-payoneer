---
phase: 04-webhooks-payee-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pkg/payoneer/webhooks.go, pkg/payoneer/webhooks_test.go]
autonomous: true
requirements: [WEB-01, WEB-02]
---

<objective>
Implement the foundation for receiving and validating Payoneer webhooks (IPCN), including HMAC SHA-256 signature verification and a convenient HTTP middleware.

Purpose: Ensure the authenticity of incoming notifications from Payoneer.
Output: `pkg/payoneer/webhooks.go` with signature validation, payload parsing, and middleware.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webhooks-payee-management/04-CONTEXT.md
@.planning/phases/04-webhooks-payee-management/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Webhook Models and Signature Verification</name>
  <files>pkg/payoneer/webhooks.go, pkg/payoneer/webhooks_test.go</files>
  <behavior>
    - ValidateSignature returns true for matching HMAC SHA-256 hex signatures.
    - ValidateSignature returns false for mismatched signatures.
    - ParseWebhook extracts the signature from X-Payoneer-Signature, validates it, and unmarshals the JSON body.
  </behavior>
  <action>
    - Create `pkg/payoneer/webhooks.go`.
    - Define `WebhookEvent` struct with fields: `EventType`, `EventID`, `Timestamp`, `Content` (json.RawMessage).
    - Implement `ValidateSignature(payload []byte, signature string, secret string) bool` using HMAC SHA-256.
    - Implement `ParseWebhook(r *http.Request, secret string) (*WebhookEvent, error)` which:
      1. Reads raw body (must not consume it for later use if possible, or provide a way to access it).
      2. Validates signature via `X-Payoneer-Signature` header.
      3. Unmarshals to `WebhookEvent`.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer -run TestWebhook</automated>
  </verify>
  <done>Signature validation and payload parsing are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Webhook Validation Middleware</name>
  <files>pkg/payoneer/webhooks.go, pkg/payoneer/webhooks_test.go</files>
  <behavior>
    - WebhookValidator middleware returns 401/403 for missing or invalid signatures.
    - WebhookValidator middleware allows the request to proceed for valid signatures.
    - Middleware ensures the request body is still readable by subsequent handlers.
  </behavior>
  <action>
    - Implement `WebhookValidator(secret string) func(http.Handler) http.Handler`.
    - The middleware should:
      1. Read the entire body to compute the HMAC.
      2. If validation fails, return `http.StatusUnauthorized`.
      3. If validation succeeds, replace `r.Body` with a new `io.NopCloser` containing the original data so downstream handlers can read it.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer -run TestWebhookMiddleware</automated>
  </verify>
  <done>HTTP middleware for automatic signature validation is implemented and tested.</done>
</task>

</tasks>

<verification>
- `ValidateSignature` handles various payload sizes and characters.
- `ParseWebhook` correctly handles missing headers.
- Middleware handles concurrent requests without data races on body reading.
</verification>

<success_criteria>
- Signature verification is cryptographically sound (HMAC SHA-256).
- Webhook payloads are parsed into structured Go types.
- Middleware provides an easy-to-use security layer for developers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-webhooks-payee-management/04-01-SUMMARY.md`
</output>
