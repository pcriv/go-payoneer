---
phase: 04-webhooks-payee-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [pkg/payoneer/payees.go, pkg/payoneer/payees_test.go, pkg/payoneer/service.go, pkg/payoneer/client.go]
autonomous: true
requirements: [PYE-01, PYE-02]
---

<objective>
Implement the PayeesService to manage the payee lifecycle, including generating onboarding registration links and tracking payee status.

Purpose: Enable users to onboard new payees and monitor their account standing.
Output: `pkg/payoneer/payees.go` with core service implementation.
</objective>

<execution_context>
@/Users/pablocrivella/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/pablocrivella/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webhooks-payee-management/04-CONTEXT.md
@.planning/phases/04-webhooks-payee-management/04-RESEARCH.md
@pkg/payoneer/client.go
@pkg/payoneer/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize PayeesService and Define Models</name>
  <files>pkg/payoneer/client.go, pkg/payoneer/service.go, pkg/payoneer/payees.go</files>
  <action>
    - Add `Payees *PayeesService` field to `Client` struct in `pkg/payoneer/client.go`.
    - Update `NewClient` in `pkg/payoneer/client.go` to initialize `Payees`.
    - Define `PayeesService` type in `pkg/payoneer/service.go`.
    - Create `pkg/payoneer/payees.go` and define structs:
      - `Payee` (representing registration details)
      - `PayeeStatus` (status code and description)
      - `RegistrationLinkRequest` (for generating onboarding URLs)
      - `RegistrationLinkResponse` (wrapping the link string)
    - Use `Optional[T]` for fields that may be omitted or null by the API.
  </action>
  <verify>
    <automated>go build ./pkg/payoneer</automated>
  </verify>
  <done>PayeesService is integrated into the client and models are defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Payee Management Operations</name>
  <files>pkg/payoneer/payees.go, pkg/payoneer/payees_test.go</files>
  <behavior>
    - RegistrationURL sends a POST to `/v4/programs/{program_id}/payees/registration-link` and returns the link.
    - GetStatus sends a GET to `/v4/programs/{program_id}/payees/{payee_id}/status` and returns the payee's current standing.
    - Get sends a GET to `/v4/programs/{program_id}/payees/{payee_id}` and returns full payee details.
  </behavior>
  <action>
    - Implement `RegistrationURL(ctx context.Context, payeeID string, opts ...RegistrationOption) (string, error)`:
      - Support functional options like `WithRedirectURL`, `WithLanguage`.
    - Implement `GetStatus(ctx context.Context, payeeID string) (*PayeeStatus, error)`.
    - Implement `Get(ctx context.Context, payeeID string) (*Payee, error)`.
    - Use `c.NewRequest` and `c.Do` for all API calls.
  </action>
  <verify>
    <automated>go test -v ./pkg/payoneer -run TestPayeesService</automated>
  </verify>
  <done>Payee registration links and status retrieval are implemented and tested.</done>
</task>

</tasks>

<verification>
- Payee registration link request payload correctly includes the `payee_id`.
- Status retrieval handles all possible Payoneer payee statuses.
- API requests use the correct ProgramID from the client.
</verification>

<success_criteria>
- Developer can generate a registration link for a new payee.
- Payee status can be monitored asynchronously.
- All operations follow the established SDK patterns (functional options, error handling).
</success_criteria>

<output>
After completion, create `.planning/phases/04-webhooks-payee-management/04-02-SUMMARY.md`
</output>
